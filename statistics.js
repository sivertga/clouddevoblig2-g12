/* 
const mongoToArray = db.IoT.find().toArray();

if (mongoToArray.length > 0) {
    printjson(mongoToArray[0]);
}

const maxOfArray = Math.max(mongoToArray);
const minOfArray = Math.min(mongoToArray);
const averageOfArray = mongoToArray.reduce((a, b) => a + b, 0) / mongoToArray.length; */